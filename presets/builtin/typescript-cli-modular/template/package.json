{
  "name": "{{packageName}}",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "{{commandName}}": "./dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests"
  },
  "engines": {
    "node": ">=24.11.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^6.0.0"
  }
}
