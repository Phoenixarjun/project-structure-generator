[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "{{projectName}}"
version = "0.1.0"
description = "FastAPI feature modular backend"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn>=0.28.0",
]
